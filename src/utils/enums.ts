export enum Route {
  Home = 'Home',
  Splash = 'Splash',
  End = 'End'
}

export enum Modal {
  Unknown = '',
  Package = 'Package',
  Story = 'Story',
  Event = 'Event',
  Switcher = 'Switcher'
}

export enum Sound {
  Eat = 'eat',
  DoorOpen = 'doorOpen',
  DoorClose = 'doorClose',
  ChestOpen = 'chestOpen',
  ChestClose = 'chestClose',
  Coin = 'coin',
  Hit = 'hit',
  End = 'end',
}

export enum EndType {
  Unknown = '',
  Health = 'health',
  Energy = 'energy'
}

export enum EffectType {
  Mask = 'mask',
  Money = 'money',
  Food = 'food'
}

export enum Period {
  Morning = 'morning',
  Noon = 'noon',
  Night = 'night'
}
